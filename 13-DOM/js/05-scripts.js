const card = document.querySelectorAll('.card');
console.log(card);

//retorna todo en si y sigue la misma sisntaxix que css

//si un elemento no existe
const noExiste = document.querySelectorAll('noExiste');
console.log(noExiste); //trae un node (arreglo) vacio